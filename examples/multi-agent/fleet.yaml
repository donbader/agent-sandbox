# yaml-language-server: $schema=.build/schema.json
agents:
  - coder
  - reviewer

shared:
  features:
    - plugin: github-pat
      token: "${GITHUB_PAT}"
    - plugin: custom-runtime
      commands:
        - "apt-get install -y ripgrep fd-find"
